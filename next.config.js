/** @type {import('next').NextConfig} */
const nextConfig = {
  // This is a Vite app, not a Next.js app
  // We're using this config only for Vercel compatibility
  webpack: false,
};

module.exports = nextConfig;
